app_version: "1.0.0"

server:
  host: "0.0.0.0"
  port: 7070

service_db:
  driver: "postgres"
  host: "0.0.0.0"
  port: 5432
  username: "emulator"
  password: "emulator123"
  database: "emulator_db"
  sslmode: "disable"
  max_connections: 10
  max_idle_connections: 5

emulator_db:
  driver: "postgres"
  host: "0.0.0.0"
  port: 5432
  username: "emulator"
  password: "emulator123"
  database: "emulator_db"
  sslmode: "disable"
  max_connections: 20
  max_idle_connections: 10

# Configuração do banco de dados WXS (sistema externo)
wxsDB:
  driver: "mssql"
  host: "172.16.17.67\\W_ACCESS"
  port: 1433
  database: "W_Access"
  username: "W-Access"
  password: "db_W-X-S@Wellcare924_"
  timeout: "30s"

# Configurações de logging
logging:
  level: "info"
  format: "json"
  enable_trace: true
  max_file_size: "50MB"
  max_files: 10
