services:
  postgres:
    image: postgres:15-alpine
    container_name: facial-emulator-db
    environment:
      POSTGRES_USER: emulator
      POSTGRES_PASSWORD: emulator123
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker-init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - facial-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U emulator -d postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: facial-emulator-app-dev
    environment:
      SERVER_HOST: "0.0.0.0"
      SERVER_PORT: "8080"
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_USER: emulator
      DB_PASSWORD: emulator123
      DB_DATABASE: emulator_db
    ports:
      - "8080:8080"
      - "4000-4999:4000-4999"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - facial-network
    restart: unless-stopped
    volumes:
      # Volume do código fonte para hot reload
      - .:/app
      - /app/tmp
      - ./logs:/app/logs
      - ./traces:/app/traces
      # Cache do Go para não redownload de módulos
      - go_modules:/go/pkg/mod

volumes:
  postgres_data:
    driver: local
  go_modules:
    driver: local

networks:
  facial-network:
    driver: bridge
